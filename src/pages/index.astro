---
import Layout from '../layouts/Layout.astro';
import { products, partners, clients } from '../data/products';
---

<Layout title="Inovasi Kode Tanpa Batas">
  <!-- Hero Section -->
  <section class="relative min-h-screen flex items-center overflow-hidden pt-20">
    <!-- Interactive Background -->
    <div class="absolute inset-0 z-0">
      <div class="absolute top-[-10%] left-[-5%] w-[45%] h-[45%] bg-syntax-accent/10 rounded-full blur-[140px] animate-pulse"></div>
      <div class="absolute bottom-[-10%] right-[-5%] w-[55%] h-[55%] bg-blue-600/10 rounded-full blur-[160px]"></div>
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full h-full bg-[radial-gradient(#ffffff05_1px,transparent_1px)] [background-size:40px_40px] opacity-30"></div>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 w-full py-20">
      <div class="flex flex-col lg:flex-row items-center gap-16 lg:gap-24">
        <div class="text-center lg:text-left flex-1 lg:max-w-2xl">
          <div class="inline-flex items-center space-x-2 px-4 py-2 rounded-full bg-white/5 border border-white/10 mb-8 reveal-on-scroll">
            <span class="relative flex h-3 w-3">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-syntax-accent opacity-75"></span>
              <span class="relative inline-flex rounded-full h-3 w-3 bg-syntax-accent"></span>
            </span>
            <span class="text-xs font-black text-gray-300 uppercase tracking-[0.2em]">Next-Gen Software Lab</span>
          </div>
          <h1 class="text-6xl md:text-8xl lg:text-[100px] font-black tracking-tighter mb-8 leading-[0.85] reveal-on-scroll">
            Berdayakan <br/>
            Bisnis <span class="gradient-text">Masa Depan</span>
          </h1>
          <p class="text-xl md:text-2xl text-gray-400 mb-12 leading-relaxed font-medium reveal-on-scroll" style="transition-delay: 200ms;">
            Platform orkestrasi digital untuk korporasi yang mengutamakan kecepatan, keamanan, dan desain visioner.
          </p>
          <div class="flex flex-wrap gap-6 justify-center lg:justify-start reveal-on-scroll" style="transition-delay: 400ms;">
             <a href="#harga" class="btn-primary">
               Mulai Eksplorasi
             </a>
             <a href="#tentang" class="px-8 py-4 rounded-2xl font-black text-lg glass border border-white/10 text-white hover:bg-white/10 transition-all flex items-center group">
               Pelajari Misi
               <svg class="w-5 h-5 ml-3 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
             </a>
          </div>
        </div>

        <!-- Interactive Hero Card -->
        <div class="flex-1 w-full lg:max-w-none reveal-on-scroll" style="transition-delay: 600ms;">
           <div class="relative group">
              <div class="absolute -inset-4 bg-gradient-to-r from-syntax-accent to-blue-600 rounded-[3rem] blur-3xl opacity-20 group-hover:opacity-40 transition-opacity"></div>
              <div class="relative glass rounded-[3rem] border border-white/10 p-1 overflow-hidden shadow-2xl transition-transform duration-700 hover:rotate-1">
                 <div class="bg-[#0D0D0F] rounded-[2.9rem] p-10 lg:p-14">
                    <div class="flex flex-col space-y-10">
                       <div class="flex justify-between items-start">
                          <div class="space-y-3">
                             <div class="w-16 h-4 bg-syntax-emerald/40 rounded-full"></div>
                             <div class="w-32 h-6 bg-white/10 rounded-full"></div>
                          </div>
                          <div class="w-14 h-14 rounded-2xl bg-white/5 flex items-center justify-center border border-white/10 overflow-hidden">
                             <img src="/logo.jpg" alt="Logo Interface" class="w-full h-full object-cover opacity-80" />
                          </div>
                       </div>
                       
                       <div class="space-y-6">
                          <div class="h-4 w-full bg-white/5 rounded-full"></div>
                          <div class="h-4 w-5/6 bg-white/5 rounded-full"></div>
                          <div class="h-4 w-2/3 bg-white/5 rounded-full"></div>
                       </div>

                       <div class="grid grid-cols-2 gap-8">
                          <div class="p-6 glass rounded-3xl border border-white/5">
                             <div class="text-2xl font-black text-white mb-1">98%</div>
                             <div class="text-[10px] text-gray-500 uppercase tracking-widest font-bold">Performa</div>
                          </div>
                          <div class="p-6 glass rounded-3xl border border-white/5">
                             <div class="text-2xl font-black text-white mb-1">0ms</div>
                             <div class="text-[10px] text-gray-500 uppercase tracking-widest font-bold">Latensi</div>
                          </div>
                       </div>
                    </div>
                 </div>
              </div>
           </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Social Proof Marquee -->
  <section class="py-24 bg-black/40 border-y border-white/5">
    <div class="max-w-7xl mx-auto px-4 mb-20 text-center">
      <h2 class="text-xs font-black text-gray-600 uppercase tracking-[0.5em] reveal-on-scroll">Mitra Strategis Global</h2>
    </div>
    
    <div class="space-y-20">
      <div class="relative flex overflow-x-hidden">
        <div class="animate-marquee whitespace-nowrap flex items-center">
          {[...partners, ...partners].map(partner => (
            <div class="mx-16 flex items-center justify-center">
              <img src={partner.logo} alt={partner.name} class="h-10 w-auto logo-filter brightness-[100]" />
            </div>
          ))}
        </div>
      </div>

      <div class="relative flex overflow-x-hidden">
        <div class="animate-marquee whitespace-nowrap flex items-center [animation-direction:reverse]">
          {[...clients, ...clients].map(client => (
            <div class="mx-16 flex items-center justify-center">
              <img src={client.logo} alt={client.name} class="h-12 w-auto logo-filter brightness-[100]" />
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section id="tentang" class="py-40 relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-32 items-center">
        <div class="relative order-2 lg:order-1 reveal-on-scroll">
           <div class="absolute -top-16 -left-16 w-64 h-64 bg-syntax-accent/20 rounded-full blur-[120px]"></div>
           <div class="relative glass aspect-[4/3] rounded-[4rem] border border-white/10 p-2 transform -rotate-2 hover:rotate-0 transition-transform duration-700">
              <img src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?auto=format&fit=crop&q=80&w=1200" alt="Tech Background" class="w-full h-full object-cover rounded-[3.8rem] opacity-50" />
           </div>
           <div class="absolute -bottom-10 -right-10 glass p-10 rounded-[3rem] border border-white/10 shadow-3xl reveal-on-scroll" style="transition-delay: 300ms;">
              <p class="text-4xl font-black text-syntax-accent mb-2">12+</p>
              <p class="text-[10px] text-gray-400 font-bold uppercase tracking-widest">Penghargaan Global</p>
           </div>
        </div>
        
        <div class="order-1 lg:order-2">
          <span class="text-syntax-accent font-black tracking-[0.4em] uppercase block mb-6 text-xs reveal-on-scroll">Filosofi Kami</span>
          <h2 class="text-5xl md:text-7xl font-black mb-10 tracking-tighter leading-tight reveal-on-scroll" style="transition-delay: 150ms;">
            Keunggulan Terletak Pada <br/> <span class="gradient-text">Detail</span>
          </h2>
          <p class="text-gray-400 text-xl mb-12 leading-relaxed font-medium reveal-on-scroll" style="transition-delay: 300ms;">
            Kami percaya bahwa kode bukan sekadar baris script, melainkan seni pemecahan masalah. Di SyntaxTrust, setiap algoritma dipahat untuk memberikan respons intuitif bagi penggunanya.
          </p>
          
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-10 reveal-on-scroll" style="transition-delay: 450ms;">
            <div class="space-y-4 group">
               <div class="w-12 h-12 rounded-2xl bg-white/5 flex items-center justify-center border border-white/10 group-hover:bg-syntax-accent transition-colors">
                  <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
               </div>
               <h4 class="text-lg font-black text-white">Privasi Prioritas</h4>
               <p class="text-gray-500 text-sm leading-relaxed">Enkripsi tingkat tinggi pada setiap pertukaran data sistem Anda.</p>
            </div>
            <div class="space-y-4 group">
               <div class="w-12 h-12 rounded-2xl bg-white/5 flex items-center justify-center border border-white/10 group-hover:bg-syntax-accent transition-colors">
                  <svg class="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
               </div>
               <h4 class="text-lg font-black text-white">Performa Ultra</h4>
               <p class="text-gray-500 text-sm leading-relaxed">Komputasi awan yang dioptimalkan untuk skalabilitas masif.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing Section -->
  <section id="harga" class="py-40 relative bg-zinc-900/30">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-24 reveal-on-scroll">
        <h2 class="text-5xl md:text-7xl font-black mb-8 tracking-tighter">Investasi <span class="gradient-text">Eksklusif</span></h2>
        <p class="text-gray-400 max-w-2xl mx-auto text-xl font-medium">Model harga transparan untuk berbagai skala ambisi bisnis.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-10 lg:gap-14 max-w-5xl mx-auto items-stretch">
        {products.map((product, idx) => (
          <div class={`glass p-12 rounded-[4rem] border-white/10 card-hover flex flex-col relative overflow-hidden group reveal-on-scroll ${product.id === 'sik-sekolah' ? 'bg-white/5 border-syntax-emerald/40 shadow-syntax-emerald/10 ring-1 ring-syntax-emerald/20' : ''}`} style={`transition-delay: ${idx * 150}ms;`}>
            {product.id === 'sik-sekolah' && (
              <div class="absolute top-8 right-8 bg-[#00A859] text-white px-5 py-1.5 rounded-full text-[10px] font-black tracking-[0.2em] shadow-lg">
                SISTEM UNGGULAN
              </div>
            )}
            
            <div class="mb-12">
               <h3 class="text-gray-500 font-black uppercase tracking-[0.3em] text-[10px] mb-4">Edisi {product.category}</h3>
               <div class="flex items-baseline space-x-1">
                 <span class="text-5xl lg:text-5xl font-black tracking-tighter text-white">{product.price}</span>
                 {product.price !== 'Custom' && <span class="text-gray-500 text-sm font-bold">/thn</span>}
               </div>
               <p class="text-gray-400 mt-6 font-medium text-sm leading-relaxed">{product.description}</p>
            </div>

            <div class="h-px w-full bg-white/5 mb-10 group-hover:bg-syntax-emerald/30 transition-colors"></div>

            <ul class="space-y-6 mb-16 flex-grow">
              {product.features.map(feature => (
                <li class="flex items-center space-x-4 text-sm font-bold text-gray-300">
                  <div class="w-6 h-6 rounded-xl bg-white/5 flex items-center justify-center text-syntax-emerald group-hover:bg-syntax-emerald group-hover:text-white transition-all">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" /></svg>
                  </div>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>

            <button class={`w-full py-5 rounded-2xl font-black transition-all flex items-center justify-center space-x-3 group/btn ${product.category === 'Pro' ? 'bg-[#00A859] text-white shadow-2xl' : 'bg-white/5 hover:bg-white/10 text-white border border-white/10'}`}>
              <span>Amankan Slot</span>
              <svg class="w-5 h-5 opacity-0 group-hover/btn:opacity-100 transform translate-x-[-10px] group-hover/btn:translate-x-0 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M14 5l7 7m0 0l-7 7m7-7H3"/></svg>
            </button>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Panduan Section -->
  <section id="panduan" class="py-40">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col md:flex-row md:items-center justify-between gap-12 mb-28 reveal-on-scroll">
        <div class="max-w-2xl">
          <span class="text-syntax-accent font-black tracking-[0.4em] uppercase block mb-6 text-xs">Pusat Sumber Daya</span>
          <h2 class="text-5xl md:text-7xl font-black tracking-tighter leading-tight">Pelajari <span class="gradient-text">Ekosistem</span> Kami</h2>
        </div>
        <a href="#" class="px-10 py-5 glass rounded-2xl text-white font-black hover:bg-white/10 transition-all border border-white/10 shrink-0">Buka Semua Dokumen</a>
      </div>

      <div class="grid grid-cols-1 gap-14">
        {products.map((product, idx) => (
          <div class="group relative reveal-on-scroll" style={`transition-delay: ${idx * 200}ms;`}>
             <div class="absolute -inset-10 bg-syntax-accent/5 blur-[100px] opacity-0 group-hover:opacity-100 transition-opacity"></div>
             <div class="relative glass rounded-[4rem] border border-white/10 p-2 overflow-hidden hover:border-syntax-accent/30 transition-all duration-700">
                <div class="grid grid-cols-1 lg:grid-cols-12 gap-0">
                   <!-- Info Content -->
                   <div class="lg:col-span-6 p-12 lg:p-20 order-2 lg:order-1 flex flex-col justify-center">
                      <div class="flex items-center space-x-6 mb-10">
                         <div class="w-16 h-16 rounded-[2rem] bg-syntax-accent/10 flex items-center justify-center text-syntax-accent border border-syntax-accent/20 group-hover:rotate-12 transition-transform">
                            <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" /></svg>
                         </div>
                         <div>
                            <span class="text-[10px] font-black text-syntax-accent uppercase tracking-[0.3em] mb-1 block">Edukasi {product.category}</span>
                            <h3 class="text-4xl font-black tracking-tighter text-white">Mastery {product.name}</h3>
                         </div>
                      </div>
                      
                      <p class="text-gray-400 mb-12 text-xl leading-relaxed italic font-medium">
                        "Eksplorasi mendalam arsitektur {product.name} untuk mengoptimalkan alur kerja digital perusahaan Anda secara presisi."
                      </p>

                      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                        <a href={product.guideFile} class="flex items-center space-x-5 glass hover:bg-white/10 p-6 rounded-3xl border border-white/5 transition-all group/link shadow-xl">
                          <div class="w-12 h-12 rounded-2xl bg-red-500/10 flex items-center justify-center text-red-500 group-hover/link:bg-red-500 group-hover/link:text-white transition-all">
                             <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M7 2a2 2 0 00-2 2v16a2 2 0 002 2h10a2 2 0 002-2V4a2 2 0 00-2-2H7zm0 2h10v16H7V4zm2 4v2h6V8H9zm0 4v2h6v-2H9zm0 4v2h4v-2H9z" /></svg>
                          </div>
                          <div>
                            <div class="text-sm font-black text-white">Panduan Teknis</div>
                            <div class="text-[10px] text-gray-500 font-bold uppercase tracking-widest mt-1">Dokumen PDF</div>
                          </div>
                        </a>

                        <a href={`https://youtube.com/watch?v=${product.youtubeId}`} target="_blank" class="flex items-center space-x-5 glass hover:bg-white/10 p-6 rounded-3xl border border-white/5 transition-all group/link shadow-xl">
                          <div class="w-12 h-12 rounded-2xl bg-red-600/10 flex items-center justify-center text-red-600 group-hover/link:bg-red-600 group-hover/link:text-white transition-all">
                            <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M19.615 3.184c-3.604-.246-11.631-.245-15.23 0-3.897.266-4.356 2.62-4.385 8.816.029 6.185.484 8.549 4.385 8.816 3.6.245 11.626.246 15.23 0 3.897-.266 4.356-2.62 4.385-8.816-.029-6.185-.484-8.549-4.385-8.816zm-10.615 12.816v-8l8 4-8 4z" /></svg>
                          </div>
                          <div>
                            <div class="text-sm font-black text-white">Kursus Video</div>
                            <div class="text-[10px] text-gray-500 font-bold uppercase tracking-widest mt-1">Tutorial 4K</div>
                          </div>
                        </a>
                      </div>
                   </div>

                   <!-- Visual Preview -->
                   <div class="lg:col-span-6 min-h-[400px] lg:min-h-0 order-1 lg:order-2 overflow-hidden">
                      <div class="w-full h-full relative group/video">
                         <div class="absolute inset-0 bg-syntax-accent/40 z-10 group-hover/video:bg-transparent transition-colors duration-700"></div>
                         <img src={`https://img.youtube.com/vi/${product.youtubeId}/maxresdefault.jpg`} alt="Tutorial" class="w-full h-full object-cover group-hover/video:scale-110 transition-transform duration-[2s]" />
                         <div class="absolute inset-0 z-20 flex items-center justify-center">
                            <button class="w-24 h-24 rounded-full glass border border-white/20 flex items-center justify-center text-white transform group-hover/video:scale-125 transition-transform duration-500 shadow-3xl">
                               <svg class="w-10 h-10 ml-1 fill-current" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
                            </button>
                         </div>
                      </div>
                   </div>
                </div>
             </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-40 relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="relative bg-white rounded-[5rem] p-16 md:p-32 overflow-hidden shadow-[0_0_100px_rgba(124,58,237,0.3)] reveal-on-scroll">
        <div class="absolute top-[-20%] right-[-10%] w-96 h-96 bg-syntax-accent/10 rounded-full blur-[100px]"></div>
        <div class="absolute bottom-[-20%] left-[-10%] w-[40rem] h-[40rem] bg-blue-600/5 rounded-full blur-[120px]"></div>
        
        <div class="relative z-10 text-center">
           <span class="text-syntax-accent font-black tracking-[0.5em] uppercase block mb-10 text-xs reveal-on-scroll">Urutan Peluncuran</span>
           <h2 class="text-6xl md:text-8xl lg:text-[110px] font-black text-black mb-12 tracking-tighter leading-[0.85] reveal-on-scroll" style="transition-delay: 150ms;">
             Siap Meluncur <br/> ke <span class="bg-gradient-to-r from-syntax-accent to-syntax-emerald bg-clip-text text-transparent italic">Masa Depan?</span>
           </h2>
           <p class="text-gray-600 text-xl md:text-2xl mb-16 max-w-3xl mx-auto font-bold reveal-on-scroll" style="transition-delay: 300ms;">
             Gabung dengan ribuan perusahaan inovatif yang telah mempercayakan infrastruktur mereka pada SyntaxTrust.
           </p>
           <button class="bg-black text-white hover:bg-[#00A859] px-16 py-7 rounded-[2rem] font-black text-2xl transition-all transform hover:scale-110 shadow-3xl hover:translate-y-[-5px] active:scale-95 reveal-on-scroll" style="transition-delay: 450ms;">
             Amankan Kontrak Anda
           </button>
        </div>
      </div>
    </div>
  </section>
</Layout>
