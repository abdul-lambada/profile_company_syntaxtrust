---
import Layout from "../layouts/Layout.astro";
import { products } from "../data/products";
---

<Layout
    title="Layanan & Solusi"
    description="Jelajahi solusi IT eksklusif dari SyntaxTrust: Sistem Informasi Keuangan Sekolah terintegrasi dan Jasa Pembuatan Website Kustom profesional."
>
    <!-- Header Section -->
    <section class="pt-48 pb-20 relative overflow-hidden">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div class="text-center max-w-4xl mx-auto">
                <span
                    class="text-syntax-emerald font-black tracking-[0.4em] uppercase block mb-6 text-xs reveal-on-scroll"
                    >Pilihan Solusi</span
                >
                <h1
                    class="text-6xl md:text-8xl font-black mb-10 tracking-tighter leading-tight reveal-on-scroll"
                >
                    Investasi <span class="gradient-text">Eksklusif</span> Untuk Masa
                    Depan
                </h1>
                <p
                    class="text-gray-400 text-xl md:text-2xl leading-relaxed font-medium reveal-on-scroll"
                    style="transition-delay: 200ms;"
                >
                    Dari sistem administrasi sekolah hingga platform web kustom
                    kelas dunia, kami hadirkan solusi IT yang berkelanjutan.
                </p>
            </div>
        </div>
        <div
            class="absolute bottom-0 left-0 w-[50%] h-[50%] bg-syntax-emerald/5 rounded-full blur-[160px] -z-10"
        >
        </div>
    </section>

    <!-- Pricing Cards Section -->
    <section class="py-32 relative">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div
                class="grid grid-cols-1 md:grid-cols-2 gap-10 lg:gap-14 max-w-5xl mx-auto items-stretch"
            >
                {
                    products.map((product, idx) => (
                        <div
                            class={`glass p-12 rounded-[4rem] border-white/10 card-hover flex flex-col relative overflow-hidden group reveal-on-scroll ${product.id === "sik-sekolah" ? "bg-white/5 border-syntax-emerald/40 shadow-syntax-emerald/10 ring-1 ring-syntax-emerald/20" : ""}`}
                            style={`transition-delay: ${idx * 150}ms;`}
                        >
                            {product.id === "sik-sekolah" && (
                                <div class="absolute top-8 right-8 bg-syntax-emerald text-white px-5 py-1.5 rounded-full text-[10px] font-black tracking-[0.2em] shadow-lg">
                                    SISTEM UNGGULAN
                                </div>
                            )}

                            <div class="mb-12">
                                <h3 class="text-gray-500 font-black uppercase tracking-[0.3em] text-[10px] mb-4">
                                    EDISI {product.category}
                                </h3>
                                <div class="flex items-baseline space-x-1">
                                    <span class="text-5xl lg:text-5xl font-black tracking-tighter text-white">
                                        {product.price}
                                    </span>
                                    {product.price !== "Custom" &&
                                        product.price !== "Nego" && (
                                            <span class="text-gray-500 text-sm font-bold">
                                                /thn
                                            </span>
                                        )}
                                </div>
                                <p class="text-gray-400 mt-6 font-medium text-sm leading-relaxed">
                                    {product.description}
                                </p>
                            </div>

                            <div class="h-px w-full bg-white/5 mb-10 group-hover:bg-syntax-emerald/30 transition-colors" />

                            <ul class="space-y-6 mb-16 flex-grow">
                                {product.features.map((feature) => (
                                    <li class="flex items-center space-x-4 text-sm font-bold text-gray-300">
                                        <div class="w-6 h-6 rounded-xl bg-white/5 flex items-center justify-center text-syntax-emerald group-hover:bg-syntax-emerald group-hover:text-white transition-all">
                                            <svg
                                                class="w-3.5 h-3.5"
                                                fill="none"
                                                stroke="currentColor"
                                                viewBox="0 0 24 24"
                                            >
                                                <path
                                                    stroke-linecap="round"
                                                    stroke-linejoin="round"
                                                    stroke-width="3"
                                                    d="M5 13l4 4L19 7"
                                                />
                                            </svg>
                                        </div>
                                        <span>{feature}</span>
                                    </li>
                                ))}
                            </ul>

                            <button
                                class={`w-full py-5 rounded-2xl font-black transition-all flex items-center justify-center space-x-3 group/btn ${product.id === "sik-sekolah" ? "bg-syntax-emerald text-white shadow-2xl" : "bg-white/5 hover:bg-white/10 text-white border border-white/10"}`}
                            >
                                <span>Hubungi Sekarang</span>
                                <svg
                                    class="w-5 h-5 opacity-0 group-hover/btn:opacity-100 transform translate-x-[-10px] group-hover/btn:translate-x-0 transition-all"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="3"
                                        d="M14 5l7 7m0 0l-7 7m7-7H3"
                                    />
                                </svg>
                            </button>
                        </div>
                    ))
                }
            </div>
        </div>
    </section>

    <!-- Custom Request CTA -->
    <section class="py-32 bg-black/40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div
                class="flex flex-col lg:flex-row items-center justify-between gap-16 reveal-on-scroll"
            >
                <div class="max-w-2xl">
                    <h2
                        class="text-4xl font-black text-white mb-6 tracking-tight"
                    >
                        Butuh Solusi Yang Lebih Spesifik?
                    </h2>
                    <p class="text-gray-400 text-lg leading-relaxed">
                        Tim arsitek solusi kami siap mendengarkan visi Anda dan
                        menerjemahkannya ke dalam cetak biru digital yang
                        efisien dan skalabel.
                    </p>
                </div>
                <a href="#" class="btn-primary shrink-0">Konsultasi Gratis</a>
            </div>
        </div>
    </section>
</Layout>
